{
  "name": "@sitekit/db",
  "version": "0.1.0",
  "private": true,
  "description": "SiteKit 資料庫層：一份模型（apps/api/prisma/schema.prisma）產生 postgres／sqlite 兩個 Prisma client；createPrisma() 依 DATABASE_URL 選用並在 SQLite 上自動轉換 Json／陣列／enum",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node scripts/gen-sqlite-schema.mjs && node scripts/prisma.mjs generate && tsc -p tsconfig.json",
    "gen:sqlite": "node scripts/gen-sqlite-schema.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "typescript": "^5.6.0"
  }
}
