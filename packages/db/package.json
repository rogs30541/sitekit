{
  "name": "@sitekit/db",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "SiteKit 資料庫層：一份模型（apps/api/prisma/schema.prisma）產生 postgres／sqlite／mysql 三個 Prisma client；createPrisma() 依 DATABASE_URL 選用，SQLite 自動轉換 Json／陣列／enum、MySQL 轉換陣列",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node scripts/gen-sqlite-schema.mjs && node scripts/gen-mysql-schema.mjs && node scripts/prisma.mjs generate && node scripts/gen-dmmf.mjs && tsc -p tsconfig.json",
    "gen:sqlite": "node scripts/gen-sqlite-schema.mjs",
    "gen:mysql": "node scripts/gen-mysql-schema.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "typescript": "^5.6.0"
  }
}
