{
  "name": "@sitekit/edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "tsc -p tsconfig.json --noEmit",
    "migrate:local": "wrangler d1 migrations apply sitekit-edge --local",
    "migrate:remote": "wrangler d1 migrations apply sitekit-edge --remote",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  }
}
